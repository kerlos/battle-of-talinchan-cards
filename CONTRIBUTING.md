# คู่มือการมีส่วนร่วม

ขอบคุณที่สนใจมีส่วนร่วมในโปรเจกต์ Battle of Talinchan Cards! คู่มือนี้จะช่วยให้คุณเข้าใจวิธีการเพิ่มหรืออัปเดตข้อมูลการ์ด

## ขั้นตอนการมีส่วนร่วม

### 1. เปิด Issue

ก่อนเริ่มทำงาน **กรุณา[เปิด issue ใหม่](https://github.com/kerlos/battle-of-talinchan-cards/issues/new)** พร้อมกับ tag ที่เหมาะสม:

- `card-data` - สำหรับการเพิ่มหรืออัปเดตข้อมูลการ์ด
- `new-card` - สำหรับการ์ดใหม่ที่ยังไม่มีในระบบ
- `update-card` - สำหรับการแก้ไขข้อมูลการ์ดที่มีอยู่แล้ว
- `bug-fix` - สำหรับการแก้ไขข้อผิดพลาดในข้อมูล
- `new-set` - สำหรับเซ็ตการ์ดใหม่

**ตัวอย่าง Issue:**

```
Title: [card-data] เพิ่มการ์ด BT09-001 ถึง BT09-050

Description:
- เซ็ต: BT09
- จำนวนการ์ด: 50 ใบ
- แหล่งข้อมูล: [ลิงก์หรือแหล่งอ้างอิง]
```

### 2. รอการอนุมัติ

รอให้ maintainer ตรวจสอบและอนุมัติ issue ของคุณก่อนเริ่มทำงาน เพื่อป้องกันการทำงานซ้ำซ้อน

### 3. Fork และ Clone Repository

```bash
# Fork repository บน GitHub ก่อน
git clone https://github.com/YOUR_USERNAME/battle-of-talinchan-cards.git
cd battle-of-talinchan-cards
```

### 4. สร้าง Branch ใหม่

```bash
git checkout -b feature/add-bt09-cards
# หรือ
git checkout -b fix/update-card-data
```

### 5. เพิ่มหรืออัปเดตข้อมูลการ์ด

#### โครงสร้างไฟล์

ข้อมูลการ์ดถูกจัดเก็บใน `src/cards/` ตามรหัสเซ็ต:
- `bt01.ts` - เซ็ต BT01
- `bt02.ts` - เซ็ต BT02
- `sd01.ts` - เซ็ต SD01
- และอื่นๆ

#### รูปแบบข้อมูลการ์ด

##### Avatar Card

```typescript
{
  name: "ชื่อการ์ด",
  type: "Avatar",
  print: "BT01-001", // รหัสการ์ด
  rare: "UR", // Rarity: 'SR', 'UR', 'PR', 'CBR', 'C', 'SCR', 'R', 'USEC'
  soi: 1, // ซอย
  cost: 3, // ค่าใช้จ่าย
  gem: 2, // อัญมณี
  power: 3, // พลัง
  symbol: "ยักษ์", // สัญลักษณ์
  color: "แดง", // สี: 'แดง', 'ฟ้า', 'เขียว', 'ม่วง', 'ไม่มีสี'
  mainEffect: "ข้อความเอฟเฟกต์หลัก",
  // ฟิลด์เพิ่มเติม (ถ้ามี)
  customLimit: 1, // จำกัดจำนวน (ถ้ามี)
  ex: "ข้อความ EX", // ข้อความ EX (ถ้ามี)
  dropRate: "1/12" // อัตราการดรอป (ถ้ามี)
}
```

##### Magic Card

```typescript
{
  name: "ชื่อการ์ด",
  type: "Magic",
  subtype: "Normal", // 'Modification', 'React', 'Normal', 'Land'
  print: "BT01-001",
  rare: "C",
  soi: 1,
  cost: 2,
  symbol: "เทพ",
  color: "ฟ้า",
  mainEffect: "ข้อความเอฟเฟกต์หลัก"
}
```

##### Life Card

```typescript
{
  name: "ชื่อการ์ด",
  type: "Life",
  print: "BT01-001",
  rare: "C",
  soi: 1,
  mainEffect: "ข้อความเอฟเฟกต์หลัก",
  favorText: "ข้อความ Favor"
}
```

##### Construct Card

```typescript
{
  name: "ชื่อการ์ด",
  type: "Construct",
  print: "BT01-001",
  rare: "R",
  soi: 1,
  cost: 2,
  gem: 1,
  power: 2,
  symbol: "หุ่นยนต์",
  color: "เขียว",
  mainEffect: "ข้อความเอฟเฟกต์หลัก"
}
```

#### การเพิ่มการ์ดใหม่

1. เปิดไฟล์ที่เกี่ยวข้องใน `src/cards/` (เช่น `bt09.ts` สำหรับเซ็ต BT09)
2. เพิ่มการ์ดใหม่เข้าไปใน array `CARDS_XXX`
3. หากเป็นเซ็ตใหม่ ให้สร้างไฟล์ใหม่และ export array

**ตัวอย่าง:**

```typescript
// src/cards/bt09.ts
import { Card } from "@/src/types/cards";

export const CARDS_BT09: Card[] = [
  {
    name: "ชื่อการ์ด",
    type: "Avatar",
    // ... ข้อมูลการ์ด
  },
  // ... การ์ดอื่นๆ
];
```

4. เพิ่ม export ใน `src/cards/index.ts`:

```typescript
export { CARDS_BT09 } from './bt09';
```

#### การอัปเดตข้อมูลการ์ด

1. ค้นหาการ์ดที่ต้องการอัปเดตในไฟล์ที่เกี่ยวข้อง
2. แก้ไขข้อมูลตามที่ระบุใน issue
3. ตรวจสอบให้แน่ใจว่าข้อมูลถูกต้องและครบถ้วน

### 6. ตรวจสอบการทำงาน

```bash
# Build โปรเจกต์
pnpm build

# รันตัวอย่างเพื่อตรวจสอบ
pnpm example:all
```

### 7. Commit และ Push

```bash
git add .
git commit -m "feat: เพิ่มการ์ด BT09-001 ถึง BT09-050"
# หรือ
git commit -m "fix: แก้ไขข้อมูลการ์ด BT01-001"

git push origin feature/add-bt09-cards
```

**รูปแบบ Commit Message:**
- `feat:` - สำหรับฟีเจอร์ใหม่ (เช่น การ์ดใหม่)
- `fix:` - สำหรับการแก้ไขข้อผิดพลาด
- `update:` - สำหรับการอัปเดตข้อมูล

### 8. สร้าง Pull Request

1. ไปที่ GitHub repository ของคุณ
2. คลิก "New Pull Request"
3. เลือก branch ที่คุณสร้างไว้
4. **อ้างอิงถึง issue** ที่เกี่ยวข้อง (เช่น `Closes #123` หรือ `Related to #123`)
5. อธิบายรายละเอียดการเปลี่ยนแปลง
6. Submit Pull Request

## หลักเกณฑ์การมีส่วนร่วม

### ข้อมูลการ์ด

- **ความถูกต้อง**: ตรวจสอบให้แน่ใจว่าข้อมูลการ์ดถูกต้องตามการ์ดจริง
- **ความครบถ้วน**: กรอกข้อมูลให้ครบถ้วนตามโครงสร้างที่กำหนด
- **รูปแบบ**: ใช้รูปแบบข้อมูลตามที่กำหนดไว้ใน `src/types/cards.ts`
- **การจัดเรียง**: จัดเรียงการ์ดตามรหัสการ์ด (print code) อย่างเป็นระเบียบ

### รหัสการ์ด (Print Code)

- รูปแบบ: `SET-NUMBER` (เช่น `BT01-001`, `SD02-010`)
- ต้องตรงกับรหัสที่ปรากฏบนการ์ดจริง
- การ์ดที่มีหลาย rarity ให้ใช้รหัสเดียวกัน แต่ระบุ `rare` ที่แตกต่างกัน

### ข้อความเอฟเฟกต์

- ใช้ข้อความตามที่ปรากฏบนการ์ดจริง
- รักษารูปแบบการพิมพ์ (ตัวพิมพ์ใหญ่-เล็ก, เครื่องหมายวรรณยุกต์)
- สำหรับสัญลักษณ์ในข้อความ ใช้รูปแบบ `{symbol ชื่อสัญลักษณ์}` (เช่น `{symbol เทพ}`)

### การทดสอบ

- Build โปรเจกต์ให้สำเร็จโดยไม่มี error
- รันตัวอย่างเพื่อตรวจสอบว่าข้อมูลถูกต้อง
- ตรวจสอบว่าการ์ดสามารถค้นหาและกรองได้ตามปกติ

## คำถามหรือข้อสงสัย

หากมีคำถามหรือข้อสงสัยเกี่ยวกับการมีส่วนร่วม กรุณา[เปิด issue](https://github.com/kerlos/battle-of-talinchan-cards/issues/new) พร้อม tag `question` หรือติดต่อ maintainer

ขอบคุณสำหรับการมีส่วนร่วม! 🎉

